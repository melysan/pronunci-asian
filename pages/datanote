// Settings 

import { useState } from "react/cjs/react.production.min";

// onChange = {(e)=> sessionStroage.setItem('mode', e.target.checked)}

// // cards = [ 
// //     { img: /},
// // // {text}
// ]

// card map > card.onclick > r.push > index.


// result page 

// {index=r.query}

// .filter
// ex. card.filter = to filter out information in the search bar

// cons [Array,setArr]= useState(cards);
//  <input onChange = {(e)=>setArr(cards.filter(o,i)= o.text.includes(e.target.value)} */}


//set up array and data > show data on screen > router to correct index > filter the searchbar


// fav=[]

// changebase(fave.push(o) arr)